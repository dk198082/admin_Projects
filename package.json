{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
   "build": "pnpm run typecheck && pnpm -r --if-present run build",
   "build:prod": "pnpm run typecheck:prod && pnpm --filter \"@workspace/api-server...\" --filter \"@workspace/admin-console...\" --if-present run build",
   "typecheck:libs": "tsc --build",
   "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
   "typecheck:prod": "pnpm run typecheck:libs && pnpm --filter \"@workspace/api-server...\" --filter \"@workspace/admin-console...\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
